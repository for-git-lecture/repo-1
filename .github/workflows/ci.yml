# GitHub Actions 워크플로우: CI (Continuous Integration)
# 코드가 푸시되거나 PR이 생성되면 자동으로 테스트를 실행합니다.
# 실습 11번의 메인 예제

name: CI

# 트리거: Push 또는 PR 생성 시
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    
    steps:
      # 1. 코드 체크아웃
      - name: Checkout code
        uses: actions/checkout@v3
      
      # 2. Node.js 설정
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      # 3. 의존성 설치
      - name: Install dependencies
        run: npm ci
      
      # 4. 테스트 실행
      - name: Run tests
        run: npm test
      
      # 5. 테스트 결과 출력
      - name: Test Summary
        if: always()
        run: echo "✅ 테스트 완료!"

  lint:
    name: Code Quality Check
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run linter
        run: npm run lint
      
      - name: Check code formatting
        run: echo "✅ 코드 품질 검사 완료!"

  build:
    name: Build Check
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Build project
        run: echo "🔨 빌드 성공! (실제 프로젝트에서는 npm run build)"
      
      - name: Upload artifacts
        run: echo "📦 빌드 결과물 업로드 완료!"


