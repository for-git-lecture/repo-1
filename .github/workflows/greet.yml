name: Welcome

on:
  pull_request:
    types: [opened]

jobs:
  greet:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    
    steps:
      - name: PR에 환영 메시지 남기기
        uses: actions/github-script@v8
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '👋 PR 감사합니다! 코드 리뷰를 시작하겠습니다. 🎉'
            })
